## Goal\nOn the admin Check-in (Door Scanner) tab, make the currently-live event instantly accessible — no scrolling needed.\n\n## Changes\n\n### `src/components/admin/AdminDoorScanner.tsx`\n\n**1. Fetch end_date_time and detect the live event**\n- Update the `admin-events-active` query to also select `end_date_time`.\n- Compute `liveEvent`: the event where `now` falls between `date_time` and the effective end (`end_date_time` if set, else `date_time + 6h` fallback — same convention used in AdminQuickActions).\n\n**2. Auto-select the live event on mount**\n- If `selectedEventId` is empty and a `liveEvent` exists, set `selectedEventId` to it automatically (one-shot effect).\n- Falls back to manual selection if no live event.\n\n**3. \